[project]
name = "cribbage-pettingzoo"
version = "0.1.0"
description = "Multi-agent cribbage environment for PettingZoo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pip>=26.1.1",
    "rlcard>=1.2.0",
    "setuptools>=82.0.1",
]
